应用层协议实现
1.HTTP服务器开发
	1.HTTP请求解析	//是指服务器接收并解析客户端发送的HTTP请求的过程
	一个HTTP请求通常由以下几部分组成：
		1.请求行:包含请求方法（GET、POST等）、请求的URI和HTTP协议版本
		GET /index.html HTTP/1.1
		
		2.请求头:包含多个键值对，提供关于请求的附加信息
		Host: www.example.com
		User-Agent: Mozilla/5.0
		Accept: text/html

		3.空行：分隔请求头和请求体

		4.请求体：可选部分，主要用于POST请求传递数据

	在C++中，解析HTTP请求通常涉及：
		·读取原始请求数据
		·按行分割请求内容
		·解析请求行获取方法和URI
		·解析请求头键值对
		·处理可能的请求体
		
	2.HTTP响应构建	//是指服务器接收并解析客户端发送的HTTP请求的过程
	一个HTTP相应包含:
		1.状态行：包含协议版本、状态码和状态描述
		HTTP/1.1 200 OK

		2.响应头：包含多个键值对，提供关于响应的元信息
		Content-Type: text/html
		Content-Length: 1234

		3.空行:分隔响应头和响应体
		
		4.响应体：实际的响应内容（如HTML页面）
		
	在C++中构建HTTP响应通常包括：
		·设置状态码和状态描述
		·添加必要的响应头
		·准备响应体内容
		·按照HTTP协议格式组装响应
		·通过socket发送给客户端
		
		示例看:./Protocol/HTTP_recall.cpp
		
	3.静态文件服务器实现
		1.概念:是一种专门用于提供静态内容（如HTML、CSS、JavaScript、图片等）的网络服务器。它不需要处理动态内容生成，而是直接返回存储在服务器上的文件
		
		2.核心功能
		·文件读取：从磁盘读取请求的文件内容。
		·MIME类型识别：根据文件扩展名确定正确的Content-Type头。
		·缓存支持：可选的缓存机制（如ETag、Last-Modified）以提高性能。
		·目录列表：可选地提供目录浏览功能。
		·范围请求：支持HTTP范围请求（Range requests）以实现断点续传
		
		3.实现步骤
		· 创建HTTP服务器：使用socket API或更高级的库（如Boost.Asio）创建TCP服务器。
		·解析HTTP请求：解析客户端发送的HTTP请求，提取请求方法和路径。
		·验证路径：确保请求路径在服务器允许的范围内，防止目录遍历攻击。
		·读取文件：使用文件I/O操作读取请求的文件内容。
		·设置响应头：包括Content-Type、Content-Length等必要的HTTP头。
		·发送响应：将文件内容和HTTP头发送回客户端
		
		4.安全考虑
		·路径验证：防止使用../等字符访问服务器根目录之外的文件。
		·文件权限：确保服务器进程有权限读取请求的文件。
		·连接限制：防止过多的并发连接耗尽系统资源。

		5. 性能优化
		·内存映射文件：对于大文件，使用内存映射可以提高读取效率。
		·发送文件：在支持的系统上，使用sendfile系统调用可以避免用户空间和内核空间之间的数据拷贝。
		·压缩：对文本文件启用gzip压缩以减少传输数据量
		
		6.示例代码:./Protocol/static_file_server.cpp
		
2.自定义协议设计
	1.协议头（Header）	//是网络通信中位于消息前部的固定或可变长度的数据块
	主要包含控制信息。常见设计要素：
		1.魔数（Magic Number）
		·固定字节序列（如0xACBF），用于快速识别协议有效性
		
		2.版本号（Version）
		·标识协议版本（如v1.0用0x01表示）
		
		3.消息类型（Message Type）
		·区分请求/响应/心跳等（如0x01=请求，0x02=响应）
		
		4.序列号（Sequence ID）
		·唯一标识请求-响应配对（4字节无符号整数）
		
		5.消息体长度（Body Length）
		·记录消息体的字节数（通常4字节存储）

	典型二进制协议头示例（14字节）：
	| 魔数(2B) | 版本(1B) | 类型(1B) | 序列号(4B) | 长度(4B) | 保留(2B) |

	2.消息体（Body）	//承载实际业务数据
	设计要点：
		1.序列化格式
        		·二进制：TLV（Type-Length-Value）结构
        		·文本：JSON/XML（需定义字段名和类型）
        		
		2.字段排列
		·固定顺序：如[userID][name][age]
		·标签化：每个字段带标识（如Protobuf的tag）
		
		3.边界处理
		·定长：所有消息体长度相同
		·变长：依赖头部的Body Length字段

	设计原则：
		1.扩展性
		·头部预留保留字段，版本号支持升级
		
		2.对齐
		·按4/8字节对齐提升处理效率（如int32从4的倍数地址开始）
		
		3.字节序
		·明确使用网络字节序（大端序）
		
		4.校验
        	·可在尾部添加CRC32校验码（不属于消息体）

	注：实际设计中需配合字节填充（padding）和位域（bit field）优化空间
	
	3.序列化与反序列化技术
	//序列化（Serialization）是将数据结构或对象转换为可存储或传输的格式（如字节流）的过程。反序列化（Deserialization）则是将序列化后的数据恢复为原始数据结构或对象的过程。
	
		用途
		1.数据持久化：将对象保存到文件或数据库中。
		2.网络传输：在分布式系统中，通过网络传输对象。
		3.进程间通信：在不同进程间传递对象。

		常见格式
		1.二进制格式：高效但可读性差，如Protocol Buffers。
		2.文本格式：可读性好但效率较低，如JSON、XML。

C++中的实现方式
	1.手动实现：通过重载<<和>>运算符或编写专门的序列化函数
	示例：./Protocol/Serialization_Deserialization.cpp
	
	2.第三方库：
		1.Boost.Serialization：支持复杂对象的序列化。
		2.Protocol Buffers：高效的二进制序列化工具。
		3.JSON库（如nlohmann/json）：用于JSON格式的序列化。

	注意事项:
		1.版本兼容性：数据结构变化时需处理旧数据的反序列化。
		2.安全性：反序列化不可信数据可能导致安全问题。
		3.性能：二进制格式通常比文本格式更快
		
	示例：（JSON）./Protocol/Serialization_Deserialization_JSON.cpp
